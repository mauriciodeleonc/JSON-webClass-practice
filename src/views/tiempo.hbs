<div class="col-md-6 offset-md-3">
    <h1>Ciudad de {{ciudad}}, {{pais}}</h1>
    <p>Coordenadas: [<b>lat: </b>{{lat}}, <b>long: </b>{{lon}}]</p>
    <p><img src='http://openweathermap.org/img/wn/{{icon}}@2x.png'> {{temp}}°C [min: {{min}}°C, max: {{max}}°C]</p>
    <p><b>Cielo: </b>{{cielo}}</p>
    <p><b>Nubosidad: </b>{{nubosidad}}%</p>
    <p><b>Presión: </b>{{presion}} hpa</p>
    <p><b>Humedad: </b>{{humedad}}%</p>
    <!--
    <div class="card-body">
        {{#if contacts}}
            <ul class='list-group'>
            {{#each contacts}}
                <li class='list-group-item d-flex justify-content-between align-items-center'>
                    {{firstname}} 
                    {{lastname}} /
                    {{email}} /
                    {{phone}}
                    <a href='/delete-contact/{{@key}}' class='btn btn-danger'>Delete</a>
                </li>
            {{/each}}
            </ul>
        {{else}}
            <p>No contacts yet</p>
        {{/if}}
    </div>
    -->
    <br>
    <hr>
    <form action='/tiempo' method="POST">
        <div class="form-group">
            <label for="ciudad">Busca el tiempo en otra ciudad</label>
            <input type='text' name='ciudad' placeholder='Ej. Monterrey' class='form-control' required>
        </div>
        <button class='btn btn-primary btn-block'>
            Mostrar el tiempo
        </button>
    </form>
</div>